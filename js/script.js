const seats = document.querySelectorAll('.seat-div');

// console.log(seats)
for (let seat of seats) {
    seat.addEventListener('click', function() {
        // console.log(seat);
        const seatName = seat.querySelectora
    });
}